<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- ACCESS_FINE_LOCATION: required for GNSS fixes (PRD.md Section 21). -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <!-- HIGH_SAMPLING_RATE_SENSORS: needed on API 31+ to request faster than
         default accel/gyro delivery, relevant once Slice 1 requests ~10 Hz. -->
    <uses-permission android:name="android.permission.HIGH_SAMPLING_RATE_SENSORS" />
    <!-- INTERNET/ACCESS_NETWORK_STATE: osmdroid (ui/map/StreetMapView.kt)
         fetches real OpenStreetMap street tiles over the network. This is
         the map BACKGROUND IMAGERY only — no sensor processing, DR math,
         or fusion logic depends on network access; the app's actual
         navigation/positioning path stays fully offline as before. -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <uses-feature android:name="android.hardware.sensor.accelerometer" android:required="true" />
    <uses-feature android:name="android.hardware.sensor.gyroscope" android:required="true" />
    <uses-feature android:name="android.hardware.location.gps" android:required="true" />

    <application
        android:label="@string/app_name"
        android:theme="@style/Theme.IDR"
        android:allowBackup="false">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
